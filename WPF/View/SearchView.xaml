<Window x:Class="WPF.View.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.View"
        mc:Ignorable="d"
        Title="SearchView" Height="475" Width="472" DataContext="{Binding Sea, Mode=OneWay, Source={StaticResource Locator}}">
    <Grid>
        <TextBlock HorizontalAlignment="Left" Height="371" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding ShowText}" VerticalAlignment="Top" Width="444"/>
        <Button Content="Wyjście" HorizontalAlignment="Left" Margin="361,404,0,0" VerticalAlignment="Top" Width="75" Command="{Binding ExitCommand, Mode=OneWay}"/>

    </Grid>
</Window>
